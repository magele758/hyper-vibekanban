---
description: Worktree 功能测试必须走 ubuntu1 隔离预览，禁止用本机 vk-start/1300x
alwaysApply: true
---

# Worktree 预览与测试（ubuntu1）

测试 **当前 git worktree / feature 分支** 时：

1. **禁止**用本机 `vk-start` / `:13001` / `:13002` / `:13000` 验证该 worktree 的改动（本机主栈留给日常主仓，避免串代码）。
2. **必须**用 SSH Host **`ubuntu1`**（`~/.ssh/config`）做隔离预览：`scripts/vk-isolate.sh preview-remote …`。
3. 远端仓库路径默认候选：`/home/shinemo/penglei/hyper-vibekanban`。若不确定或探测失败，**先问用户**再部署，不要猜其他路径、不要写死 IP。
4. 流程：当前 worktree 根执行 → push 当前分支 → 远端同名分支 checkout/pull → `preview-remote up` → 回报 Tailscale URL（约 `:23000` UI / `:28082` Relay），**不要动本机 1300x**。
5. 远端 `git fetch` 失败时，询问是否改用 `VK_PREVIEW_SYNC_METHOD=rsync`。

参考：`.cursor/skills/vk-worktree-preview/SKILL.md`
